// Generated by Rhino Blueprint — do not edit directly.
// Source: .rhino/blueprints/tasks.yaml
// NOTE: this spec imports `buildEnv` from the library test helper.
// For your app, replace the import with your own test helper or the
// published helper re-exported from `@rhino-dev/rhino-nestjs/testing`.
import { buildEnv } from '../../test/helpers/make-controller';
import type { RhinoConfig } from '@rhino-dev/rhino-nestjs';

function makeConfig(overrides: Partial<RhinoConfig> = {}): RhinoConfig {
  return {
    models: {
      tasks: {
        model: 'task',
        belongsToOrganization: false,
        softDeletes: true,
        hasAuditTrail: true,
      },
    },
    ...overrides,
  };
}

/** Build a user with a role + permissions for the target organization. */
function makeUser(role: string, perms: string[] = [], orgId = 1) {
  return {
    id: 1,
    userRoles: [
      { organizationId: orgId, permissions: perms, role: { slug: role } },
    ],
  };
}

const sampleRow = {
  "id": 1,
  "title": "sample text",
  "description": "sample text",
  "status": "sample text",
  "priority": "sample text",
  "estimatedHours": 1.5,
  "dueDate": "2024-01-01T00:00:00Z",
  "projectId": 1,
  "assignedTo": 1
};
const adminPerms = ["tasks.index","tasks.show","tasks.store","tasks.update","tasks.destroy","tasks.trashed","tasks.restore"];

describe('Task resource', () => {
  // -----------------------------------------------------------------
  // CRUD happy path
  // -----------------------------------------------------------------

  it('index returns records for the current org', async () => {
    const env = buildEnv(makeConfig(), { task: [sampleRow] });
    const user = makeUser('admin', adminPerms);
    const req = { user, organization: { id: 1 } } as any;
    const res: any = await env.controllers.global.index('tasks', {}, req);
    expect(res).toBeDefined();
  });

  it('show returns a single record by id', async () => {
    const env = buildEnv(makeConfig(), { task: [{ ...sampleRow, id: 1 }] });
    const user = makeUser('admin', adminPerms);
    const req = { user, organization: { id: 1 } } as any;
    const res: any = await env.controllers.global.show('tasks', '1', {}, req);
    expect(res).toMatchObject({ id: 1 });
  });

  it('store persists a new record', async () => {
    const env = buildEnv(makeConfig());
    const user = makeUser('admin', adminPerms);
    const req = { user, organization: { id: 1 } } as any;
    const body = {"title":"sample text","description":"sample text","status":"sample text","priority":"sample text","estimatedHours":1.5,"dueDate":"2024-01-01T00:00:00Z","projectId":1,"assignedTo":1};
    await env.controllers.global.store('tasks', body, req);
    const rows = env.client._data.task ?? [];
    expect(rows.length).toBeGreaterThan(0);
  });

  it('update modifies an existing record', async () => {
    const env = buildEnv(makeConfig(), { task: [{ ...sampleRow, id: 1 }] });
    const user = makeUser('admin', adminPerms);
    const req = { user, organization: { id: 1 } } as any;
    const body = {"title":"sample text"};
    const res: any = await env.controllers.global.update('tasks', '1', body, req);
    expect(res).toBeDefined();
  });

  it('destroy removes (or soft-deletes) the record', async () => {
    const env = buildEnv(makeConfig(), { task: [{ ...sampleRow, id: 1 }] });
    const user = makeUser('admin', adminPerms);
    const req = { user, organization: { id: 1 } } as any;
    await env.controllers.global.destroy('tasks', '1', req);
    const rows = env.client._data.task ?? [];
    const active = rows.filter((r: any) => !r.deletedAt);
    expect(active).toHaveLength(0);
  });

  // -----------------------------------------------------------------
  // 403 — role without store permission
  // -----------------------------------------------------------------

  it('rejects store from a role without store permission', async () => {
    const env = buildEnv(makeConfig());
    const user = makeUser('manager', ["tasks.index","tasks.show","tasks.store","tasks.update"]);
    const req = { user, organization: { id: 1 } } as any;
    const body = {"title":"sample text","description":"sample text","status":"sample text","priority":"sample text","estimatedHours":1.5,"dueDate":"2024-01-01T00:00:00Z","projectId":1,"assignedTo":1};
    // Without `tasks.store` in permissions, the policy check will reject.
    // The exact exception type depends on how the route is guarded;
    // here we assert store throws (not persists).
    await expect(
      env.controllers.global.store('tasks', body, req),
    ).rejects.toBeDefined();
  });

});
